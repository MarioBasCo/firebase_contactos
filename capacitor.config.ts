import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.stip.ionic.contactos',
  appName: 'mis-contactos',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
